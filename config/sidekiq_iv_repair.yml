# iv_repair_worker: recent IV tip / tip-sync hollow-middle gap fills
# (queue: iv_repair). Own process so cold history backfill cannot starve
# IV completeness. 512MB eco dyno — single thread.
:concurrency: 4
:queues:
  - iv_repair
  - iv_repair_scar
